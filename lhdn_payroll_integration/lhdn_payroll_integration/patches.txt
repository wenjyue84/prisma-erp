[pre_model_sync]
# Patches added in this section will be executed before doctypes are migrated
# Read docs to understand patches: https://frappeframework.com/docs/v14/user/en/database-migrations

[post_model_sync]
# Patches added in this section will be executed after doctypes are migrated

# US-024 migration note (manual data fix — no auto patch needed):
# Employee records with custom_id_type = 'Business Registration Number' must be
# updated to 'BRN' to match the revised Select field options and LHDN schemeID vocabulary.
# Run the following in bench console to identify affected records:
#   frappe.db.get_all("Employee", filters={"custom_id_type": "Business Registration Number"}, fields=["name", "employee_name"])
# Then update each with:
#   frappe.db.set_value("Employee", name, "custom_id_type", "BRN")
#   frappe.db.commit()